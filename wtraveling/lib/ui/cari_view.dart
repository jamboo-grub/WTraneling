
import 'package:flutter/material.dart';

class Cari extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("makan"),),
      body: ListView(
        children: <Widget>[
         
        ],
      ),
    );
  }
}